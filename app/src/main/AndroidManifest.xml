<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dpearth.dvox">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/dies_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/dies_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectDIES">
        <activity android:name="com.dpearth.dvox.LoginActivity"
            android:theme="@style/LoginTheme.ProjectDIES"
            android:windowSoftInputMode="stateUnchanged|adjustPan"
            android:screenOrientation="portrait">
        <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.dpearth.dvox.MainActivity"
            android:theme="@style/LoginTheme.ProjectDIES"
            android:windowSoftInputMode="stateUnchanged|adjustPan"
            android:screenOrientation="portrait">
        </activity>
    </application>
</manifest>